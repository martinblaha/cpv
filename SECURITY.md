# Security Policy

## API Key Storage

Your Anthropic API key is stored locally in your browser using `chrome.storage.local`. It is never sent anywhere except directly to the Anthropic API (`api.anthropic.com`).

## Reporting a Vulnerability

If you discover a security vulnerability, please report it responsibly by emailing the maintainer directly rather than opening a public issue.
